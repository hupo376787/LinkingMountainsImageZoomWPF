<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:LinkingMountains.ImageZoom">
    <Style TargetType="{x:Type local:ImageZoom}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ImageZoom}">
                    <ControlTemplate.Resources>
                        <local:PercentageFormatConverter x:Key="PercentageFormatConverter"/>
                    </ControlTemplate.Resources>
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            ClipToBounds="True">
                        <Grid x:Name="PART_ImageContainer" Background="Transparent">
                            <Image x:Name="PART_Image" Source="{TemplateBinding Source}" Stretch="Uniform" IsHitTestVisible="False">
                                <Image.RenderTransformOrigin>0.5,0.5</Image.RenderTransformOrigin>
                                <Image.RenderTransform>
                                    <TransformGroup>
                                        <ScaleTransform x:Name="PART_ScaleTransform"/>
                                        <TranslateTransform x:Name="PART_TranslateTransform"/>
                                    </TransformGroup>
                                </Image.RenderTransform>
                            </Image>                            
                            <Border x:Name="PART_ScaleTextBorder" Width="Auto" Height="30" CornerRadius="15" Background="#a0000000" BorderThickness="0"
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="{x:Type Border}">
                                        <Setter Property="Opacity" Value="0"/>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding ElementName=PART_ScaleTransform,Path=ScaleX,Mode=OneWay,Converter={StaticResource PercentageFormatConverter}}" 
                                           Foreground="White" FontSize="15" Margin="15 0" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>
